1.
malloc 申请的空间不会和已有的空间重合，memcpy只会在自己使用的时候才会出错
memove的作用是如果人为出现了错误（地址有重合）他也可以避免移动出错 
循环队列的扩容操作 先malloc一个空间在移动过去
2.
循环队列中 使用tail = 0 的好处在于在遍历的时候可以把i  != tail 作为结束条件 
当我们结束条件和开始条件相同的时候可以使用do while格式

被解救的姜戈

只有提升打代码的极限速度 才能留出更多的时间给思考 
