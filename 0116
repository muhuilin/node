1.
优先队列不是队列
队列的本质：
队列适合处理具有先后顺序的问题
树是一种思维方式 看待问题的方式
在树的遍历中 我们用队列来处理层序遍历 
依次访问树的子节点 -> 隐藏的是有顺序
栈适合处理具有完全包含关系的问题

问题求解树 ？

中间层的应用

系统栈在出栈的时候也不会清空原来上的值 是逻辑上的出栈，原来的值还在
--------》   写程序验证
树的节点代表集合 
边带边关系

完全包含问题 -》栈
非完全包含关系 -》多线程

在根节点可以访问整棵树的信息
但是在其他节点都只能访问树的一个子集

栈的本身还在包含顺序

同一个进程 的不同线程的资源相互影响
线程也是资源 线程对应资源的上线通常是 8M -》栈空间

局部变量存放在栈区 无法开辟大空间的

一个进程有多个线程 每个线程都是 8M 
系统把空间变成了栈空间 分配给线程
